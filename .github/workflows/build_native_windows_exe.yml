# This workflow will build and test gbsplay in various configurations on Windows.

name: Windows Build

on:
  push:
  pull_request:
    types: [opened]
  schedule:
    - cron: '23 4 5 * *'

jobs:
  build-windows:
    name: Build and test on Windows

    runs-on: windows-latest

    strategy:
      matrix:
        type: [MINGW64]
        flags: ['LDFLAGS=-static']
        include:
          - type: MINGW64
            msystem: true
            packages: mingw-w64-x86_64-toolchain groff
            plugouts: dsound
            shell: msys2 {0}

    defaults:
      run:
        shell: ${{ matrix.shell }}

    steps:
    - uses: actions/checkout@v4
    - if: ${{ matrix.msystem }}
      name: Setup MSYS2
      uses: msys2/setup-msys2@v2
      with:
        msystem: ${{ matrix.type }}
        install: git base-devel ${{ matrix.packages }} zlib-devel dos2unix
        release: false
        update: false
    - name: Build and test
      env:
        CONFIGURE_FLAGS: ${{ matrix.flags }} --enable-verbosebuild --build=x86_64-w64-mingw32
      run: |
        make
    - name: Generate manpage
      run: |
        version=$(sed -n "/^VERSION/s/.* //p" config.mk)
        output_dir=gbsplay-${version}
        mkdir ${output_dir}/
        cp -v gbsplay.exe gbsinfo.exe ${output_dir}/
        for manpage in gbsinfo gbsplay gbsplayrc; do
            groff -P-b -P-u -man -Tascii man/${manpage}.? > ${output_dir}/${manpage}.txt
        done
        grep -v '^\[!\[' README.md > ${output_dir}/troff-ascii.README.txt
        echo "output_dir=${output_dir}" >> "$GITHUB_ENV"
    - name: upload binary
      uses: actions/upload-artifact@v4
      with:
        name: gbsplay windows
        path: ${{ env.output_dir }}
        if-no-files-found: error
        retention-days: 7 # don't hog resources, real builds are attached to the release anyway
        compression-level: 9 # we seldom release, so try as hard as we can
